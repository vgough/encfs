language: cpp

matrix:
  include:
    - os: linux
      compiler: gcc
      dist: trusty
      sudo: required
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
          packages:
            - attr
            - fuse
            - libfuse-dev
            - gettext
            - cmake3
      env:
        - SUDO_TESTS=true

    - os: linux
      compiler: gcc
      dist: trusty
      sudo: required
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
          packages:
            - attr
            - fuse
            - libfuse-dev
            - gettext
            - cmake3
      env:
        - SUDO_TESTS=true

    - os: linux
      compiler: gcc
      dist: trusty
      sudo: required
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
          packages:
            - attr
            - fuse
            - libfuse-dev
            - gettext
            - cmake3
      env:
        - SUDO_TESTS=true

    - os: linux
      compiler: gcc
      dist: trusty
      sudo: required
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
          packages:
            - attr
            - fuse
            - libfuse-dev
            - gettext
            - cmake3
      env:
        - SUDO_TESTS=true

before_script:
  - ./ci/setup.sh

script:
  - ./build.sh
