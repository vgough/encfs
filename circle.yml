machine:
  timezone:
    America/Los_Angeles

dependencies:
  pre:
    - sudo apt-get update; sudo apt-get install attr fuse libfuse-dev gettext

test:
  override:
    - ./ci/setup.sh
    - ./ci/build.sh
